string=input("enter a string:")
def railfence(txt):
    result=""
    for i in range(len(txt)):
        if i % 2 ==0:
            result +=txt[i]
    for i in range(len(txt)):
        if i % 2 !=0:
            result +=txt[i]
    return result
print("encrypted:",railfence(string))
